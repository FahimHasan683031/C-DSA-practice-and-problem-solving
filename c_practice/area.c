#include<stdio.h>
int main(){
    int length,width;
    printf("Enter the value of length");
    scanf("%d",&length);

    printf("Enter the value of width");
    scanf("%d", &width);


    printf("Area is : %d", length*width);
}